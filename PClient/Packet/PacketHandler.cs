/*using PClient;
using System;


    class PacketHandler
    {
        // 어떤세션에서 조립 되었는가. 
        public static void SP_ResultHandler(PacketSession session, IPacket packet) 
        {
        SP_Result resultPacket = packet as SP_Result;
        ServerSession serverSession = session as ServerSession;

        if (resultPacket.result == true)
        {

        }
            
    }
      



}
*/